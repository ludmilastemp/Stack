#include <stdio.h>
#include <stdlib.h>
#include <assert.h>

#include "error.h"

int StackCtor (Stack* stk, size_t capacity = 8);

int main()
{
    struct Stack stk = {};

    if (!StackCtor(&stk)) return 0;
    StackDump (&stk, __FILE__, __LINE__);

    return 0;
}

int StackCtor (Stack* stk, size_t capacity)
{
    if (!stk) return 0;

    stk->size = 0;
    stk->capacity = capacity;
    stk->data = (int*) calloc (stk->capacity, sizeof (int));

    if (!StackErr) return 1;
}

int StackDtor (Stack* stk)
{
    
}


//int Push
